/*
#include "rng.h"

namespace {
	std::uniform_real_distribution<number> s_distribution(0, 1);
}

rng::rng(unsigned int seed) :
	m_generator(seed)
{
}

number rng::operator()()
{
	return s_distribution(m_generator);
}
*/
